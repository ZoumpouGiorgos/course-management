<% unread = user.notifications.unread.count %>
<% if unread > 0 %>
  <span class="absolute -top-1 -right-1 text-[11px] px-2 py-0.5 rounded-full bg-red-600 text-white">
    <%= unread %>
  </span>
<% end %>
